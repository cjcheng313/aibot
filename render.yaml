services:
  - type: web
    name: ai-store-assistant-mvp
    env: python
    plan: starter
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn your_application.wsgi:application --bind 0.0.0.0:$PORT"
    autoDeploy: true
    healthCheckPath: /healthz
